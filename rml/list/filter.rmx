// val filter : ('a -> bool) -> 'a list -> 'a list

/* [filter p l] is [l] with all the members [v] removed if [p v] is [false].
 * Order is preserved. */
let rec filter p l =
  match l with 
  |[] -> []
  |h::t -> if p h then h::(filter p t) else filter p t
  end
  // TODO: Implement me!
in
